pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // Needed to automatically enforce Java Launchers/Compilers
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'CleanroomGradle'
